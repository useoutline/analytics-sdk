import { init } from '@/methods/init'
import type {
  InitOptions,
  AnalyticsEvent,
  PageData,
  AnalyticsEvents,
} from '@/types'

export default init
export type { InitOptions, AnalyticsEvent, PageData, AnalyticsEvents }
